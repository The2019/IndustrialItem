version: '3'

services:
  webapp:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./instance:/app/instance
      - ./uploads:/app/uploads
    environment:
      - FLASK_APP=app.py
      - FLASK_ENV=development
      - SECRET_KEY=docker-industrial-item-secret
    restart: unless-stopped